import { SplitPricePipe } from './split-price.pipe';

describe('SplitPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitPricePipe();
    expect(pipe).toBeTruthy();
  });
});
